<nav class="navbar navbar-light bg-white nav-dark sticky-top">
   <div class="container-fluid">
    <%= link_to "/", class: "remove-text" do %><h3 class="navbar-title headpage">FoodService.com</h3><%end%>
    <%if @current_user%>
      <div class="nav-items">
        <%= image_tag "user.png", :class => "user-image" %>
        <%= link_to @current_user.name.capitalize() , "/users/#{@current_user.id}", class: "link-tag text-dark profile"%>
        <%= form_tag(destroy_session_path , method: "delete",class: "navbar-items link-tag ") do %>
        <%= submit_tag("Signout" , :class => "btn btn-outline-dark") %>
        <% end %>
      </div>
    <%end%>
  </div>

</nav>

<div class="main-menu">
  <ul>
    <section class="home">
      <%= render 'layouts/setnavbar'%>
    </section>
  </ul>
</div>
